# macOS / RStudio / editors
.DS_Store
.Rproj.user/
.Rhistory
.RData
.Ruserdata
.claude/
.positai

# {targets} cache (rebuilt by targets::tar_make())
_targets/

# LaTeX build files (the compiled paper/main_paper.pdf is versioned)
paper/*.aux
paper/*.bbl
paper/*.blg
paper/*.fdb_latexmk
paper/*.fls
paper/*.log
paper/*.out
paper/*.toc
paper/*.synctex.gz

# External raster data, too large for the repository (see data/README.md)
data/raw/mapbiomas/*.tif

# Local files that are not part of the replication package
/_archive/
/ABOUT/
